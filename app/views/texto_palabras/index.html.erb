<h1>Listing texto_palabras</h1>

<table>
  <thead>
    <tr>
      <th>Id tp</th>
      <th>Id texto</th>
      <th>Id palabrab</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @texto_palabras.each do |texto_palabra| %>
      <tr>
        <td><%= texto_palabra.id_tp %></td>
        <td><%= texto_palabra.id_texto %></td>
        <td><%= texto_palabra.id_palabrab %></td>
        <td><%= link_to 'Show', texto_palabra %></td>
        <td><%= link_to 'Edit', edit_texto_palabra_path(texto_palabra) %></td>
        <td><%= link_to 'Destroy', texto_palabra, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Texto palabra', new_texto_palabra_path %>
